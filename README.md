# 🎲 Générateur aléatoire de lettres en Python

Ce projet est un petit script Python qui génère des lettres aléatoires jusqu’à ce que la lettre **"w"** soit tirée.

---

## 📌 Description

Le programme utilise :
- le module `random` pour choisir une lettre au hasard
- le module `string` pour accéder à l’alphabet (minuscules et majuscules)

La boucle continue tant que la lettre tirée n’est pas **"w"**.

---

## 🧠 Fonctionnement du code

```python
import random
import string
